#likely this file will be removed in the future, but for now just ignore it
*/commits.json

#pesky osx .DS_Stores
*/.DS_Store
.DS_Store

#this is a folder used by the repo miner crystalgazer, which requests that it be gitignored
.cg

#for now ignore the history.txt since the index.js file isn’t committing it in the right #order (see line~327)
*/history.txt